{
  "name": "uav-digital-twin-deception",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based UAV digital twin deception lab prototype using React, Axios, Node.js, Express, and MongoDB.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -k \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}

